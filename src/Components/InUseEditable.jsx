import { Tr, Th, Input } from "@chakra-ui/react";

export default function InUseEditable({ editFormData }) {
  return;
  <Tr>
    <Th>
      <Input />
    </Th>
    <Th>
      <Input />
    </Th>
    <Th>
      <Input />
    </Th>
  </Tr>;
}
